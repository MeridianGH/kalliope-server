name: ESLint

on:
  - push
  - pull_request

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Node Dependencies
        run: npm install

      - name: Run ESLint
        run: npx eslint .
